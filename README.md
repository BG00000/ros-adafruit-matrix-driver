# ROS Adafruit Matrix Driver

## Installation

* Clone this repo
* Source ROS2
* `rosdep update && rosdep install --from-paths . -y` to install deps
* `source ./install/setup.sh` to source the install
* `ros2 launch ./launch/launch.py` to start the node